"""群聊可见事件记录面（记录 + 渲染 + 装配落点）。

与会话历史（``app/llm/session.py``）是**两套独立内容**：

- 会话历史 = 规范对话形态，只在消息触发机器人时写入；
- 本包 = 群聊最近可见事件流（含表情/戳/撤回/我的发言），在事件接收时持续记录，
  装配时按窗口渲染成环境背景。

模块划分：``events`` 契约、``store`` 存储、``render`` 渲染。
"""
