# ==================== 运行模式 ====================
# debug=true 时日志输出更详细、WebUI 绑定 127.0.0.1
DEBUG=false

# ==================== WebUI ====================
# 建议仅本机访问；对外暴露请使用反向代理并设置 WEBUI_TOKEN
WEBUI_HOST=127.0.0.1
WEBUI_PORT=9200
# 访问 WebUI 所需的 Bearer Token；留空则不启用鉴权
WEBUI_TOKEN=123456

# ==================== 数据 / 日志 ====================
# SQLite 数据库文件路径（相对项目根目录）
DB_PATH=data/app.db
# 日志目录
LOG_DIR=logs

# ==================== Bot 连接 ====================
# 首次连接超时（秒）
WS_CONNECT_TIMEOUT=30
# 心跳间隔（秒）
WS_PING_INTERVAL=30
# 心跳超时（秒）
WS_PING_TIMEOUT=10

# ==================== 调度器 ====================
# 定时任务（time_core）检查间隔（秒）
SCHEDULER_INTERVAL=30

# ==================== 模块目录 ====================
# 模块根目录（相对项目根目录；含 modules/ 插件主体、configs/ 迁移源、data/ 插件数据）
MODULE_DIR=module
