# QQBot Next 依赖清单
# 与 pyproject.toml [project.dependencies] / [project.optional-dependencies] 保持一致
# 安装：pip install -r requirements.txt

# ==================== 运行时依赖 ====================
fastapi>=0.115
uvicorn[standard]>=0.23
websockets>=12
aiohttp>=3.8
brotlicffi>=1.1          # aiohttp 3.13 + 官方 brotli 包解 br 编码不兼容，需 brotlicffi
curl_cffi>=0.15          # 浏览器指纹模拟 HTTP 客户端（模块网络请求统一走 CurlCffiClient）
aiosqlite>=0.19
pydantic>=2.4
pydantic-settings>=2.0
jinja2>=3.1
python-multipart>=0.0.6
python-dotenv>=1.0

# ==================== 开发依赖（测试） ====================
pytest>=7.4
pytest-asyncio>=0.21
